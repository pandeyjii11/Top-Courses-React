import React from "react";

const Navbar = () => {

    // Creating the navbar component and then returing it
    return(
        <div>
            <nav className="bg-bgDark py-4">
                <h1 className="text-3xl text-bold text-white text-center">
                    Top Courses
                </h1>
            </nav>
        </div>
    );
}

export default Navbar;